Basic sample Nodejs server provides Restful API
------------------------------------------------

##Quick start
- `npm i && npm start` or `yarn && yarn start`

##Docker deployment
- `docker-compose up`
